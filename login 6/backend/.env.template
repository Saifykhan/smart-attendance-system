# Copy this file to `.env` and fill values for MongoDB Atlas deployment.
# Example Atlas URI:
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.abcd.mongodb.net/?retryWrites=true&w=majority
# DB_NAME=smart_attendance_db

# Local development defaults (you can leave these for local MongoDB)
# MONGO_URI=mongodb://localhost:27017
# DB_NAME=smart_attendance_db

# JWT secret (override for production)
# JWT_SECRET=replace-me-with-a-secure-value
